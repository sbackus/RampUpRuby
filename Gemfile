source 'https://rubygems.org'

ruby '1.9.3'

gem 'activesupport' # can also add version by have , "1.0" as an example
gem 'cmath'

#running bundle instal will look at this file and install
#the gems in this file.

#gem files can help handle libary like issue you can see
#in VBA projects where libararies are needed to run specific spreadsheets
